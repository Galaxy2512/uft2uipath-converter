# Package for exploring an extracted UFT project folder before conversion:
# categorizing files by type (project_discovery.py), inspecting individual
# XML files (xml_inspector.py), and rolling both up into a single
# DiscoveryReport (discovery_report.py).
