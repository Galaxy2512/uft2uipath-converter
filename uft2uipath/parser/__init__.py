# uft2uipath.parser package: turns UFT source material (decoded ALM database
# rows, extracted ProjRep files, or generic nested project dicts) into the
# neutral AST entities (Project, UftTestCase, BusinessComponent, Step) plus
# a hand-written VBScript parser used to interpret embedded UFT script code.
